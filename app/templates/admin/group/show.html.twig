{% extends 'base.html.twig' %}

{% block title %}Class{% endblock %}

{% block body %}
    <h1 class="my-5 text-center">Class</h1>

    <div class="row">
        <div class="col-md-2">
            <a href="{{ path('admin_group_index') }}" class="btn btn-secondary"><< back</a>
        </div>
        <div class="col-md-8">
            <div class="row mb-5">
                <a href="{{ path('admin_group_edit', {'id': group.id}) }}" class="btn btn-info mx-3">edit</a>
                {{ include('admin/group/_delete_form.html.twig') }}
            </div>
            <table class="table table-striped table-bordered">
                <tbody>
                    <tr>
                        <th>Name</th>
                        <td>{{ group.name }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>

{% endblock %}
