{% extends 'base.html.twig' %}

{% block title %}Subject{% endblock %}

{% block body %}
    <h1 class="my-5 text-center">Subject</h1>

    <div class="row">
        <div class="col-md-2">
            <a href="{{ path('admin_subject_index') }}" class="btn btn-secondary"><< Back</a>
        </div>
        <div class="col-md-8">
            <div class="row mb-5">
                {{ include('admin/subject/_delete_form.html.twig') }}
            </div>
            <table class="table table-striped table-bordered">
                <tbody>
                    <tr>
                        <th>Name</th>
                        <td>{{ subject.name }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
