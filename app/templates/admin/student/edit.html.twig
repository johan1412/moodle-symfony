{% extends 'base.html.twig' %}

{% block title %}Edit Student{% endblock %}

{% block body %}
    <h1 class="my-5 text-center">Edit Student</h1>

    <div class="row">
        <div class="col-md-2">
            <a href="{{ path('admin_student_index') }}" class="btn btn-secondary"><< Back</a>
        </div>
        <div class="col-md-8">
            {{ include('admin/student/_form.html.twig', {'button_label': 'Update'}) }}
            </br>
            {{ include('admin/student/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
