{% extends 'base.html.twig' %}

{% block title %}New Student{% endblock %}

{% block body %}
    <h1 class="my-5 text-center">Create new Student</h1>

    <div class="row">
        <div class="col-md-2">
            <a href="{{ path('admin_student_index') }}" class="btn btn-secondary"><< Back</a>
        </div>
        <div class="col-md-8">
            {{ include('admin/student/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
